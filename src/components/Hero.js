import React from "react";
import { Box, Typography } from "@material-ui/core";

const Hero = () => {
  return (
    <Box height={500}>
      <Typography variant="h2">Hi, I'm Jasper</Typography>
    </Box>
  );
};

export default Hero;
